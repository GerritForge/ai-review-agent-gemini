{
  "name": "gerrit-gemini-ai",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --target=es2020 --outfile=dist/gerrit-gemini-ai.js"
  },
  "devDependencies": {
    "@gerritcodereview/typescript-api": "^3.11.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
